# Exact-pinned for reproducible, reviewable builds. Renovate (renovate.json)
# opens a PR on each new release; bumps are CI-tested before they ship.
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
jinja2==3.1.6
pypdf==6.18.1
pillow==12.3.0
reportlab==5.0.1
pymupdf==1.28.2
ebooklib==0.20
